{
  "name": "studio-rj",
  "private": true,
  "scripts": {
    "backend:install": "npm install --prefix backend",
    "backend:migrate": "npm run migrate:all --prefix backend",
    "backend:check": "npm run check:db --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "frontend:install": "npm install --prefix frontend",
    "frontend:dev": "npm run dev --prefix frontend",
    "setup": "npm run backend:install && npm run frontend:install && npm run backend:migrate"
  }
}